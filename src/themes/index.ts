import { colors } from './colors.ts';

const theme = {
  components: {
    Form: {
      itemMarginBottom: 16,
    },
    Typography: {
      titleMarginBottom: 0,
    },
  },
  token: {
    colorPrimary: colors.primary,
  },
};
export default theme;
